//
// Enable support for custom/specialized configs...

lockPref("general.config.filename", "phoenix.cfg");
lockPref("general.config.obscure_value", 0);
lockPref("general.config.vendor", "phoenix");

/// If a remote AutoConfig is being used, ensure identifying info is never shared...
lockPref("mail.identity.useremail", "");
lockPref("autoadmin.append_emailaddr", false);

/// Configure the behavior of remote autoconfig files (if active)
defaultPref("autoadmin.failover_to_cached", true);
defaultPref("autoadmin.offline_failover", true);
defaultPref("autoadmin.refresh_interval", 60);

lockPref("browser.phoenix.cfg.core.status", "031");

lockPref("browser.phoenix.cfg.status", "successfully applied :D");
